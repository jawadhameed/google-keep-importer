{
  "manifest_version": 2,

  "name": "Google Keep Importer",
  "description": "Allows you to import backup file from Google Takeout into Google Keep as notes. Just select the file you want to import.",
  "version": "1.1",

  "browser_action": {
    "default_icon": "icon.png",
    "default_popup": "popup.html"
  },
  "permissions": [
    "activeTab",
    "storage"
  ]
}